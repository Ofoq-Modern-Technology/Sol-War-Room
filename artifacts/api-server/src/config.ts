// ─────────────────────────────────────────────────────────────────────────────
// Build-time configuration
// Edit these values before running the release build.
// Each value can still be overridden at runtime via the matching env var.
// ─────────────────────────────────────────────────────────────────────────────

/** License Manager product ID for this deployment.
 *  Override at runtime with: LICENSE_PRODUCT_ID=<id>
 */
export const LICENSE_PRODUCT_ID = "1";

/** Base URL of the license server.
 *  Override at runtime with: LICENSE_SERVER_URL=<url>
 */
export const LICENSE_SERVER_URL = "https://license.ofoq.om";
